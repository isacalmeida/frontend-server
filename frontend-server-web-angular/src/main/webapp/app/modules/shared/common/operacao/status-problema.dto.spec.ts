import { StatusProblemaDTO } from './status-problema.dto';

describe('StatusProblemaDTO', () => {
    it('should create an instance', () => {
        expect(new StatusProblemaDTO()).toBeTruthy();
    });
});
