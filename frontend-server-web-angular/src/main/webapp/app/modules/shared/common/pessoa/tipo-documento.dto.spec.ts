import { TipoDocumentoDTO } from './tipo-documento.dto';

describe('TipoDocumentoDTO', () => {
    it('should create an instance', () => {
        expect(new TipoDocumentoDTO()).toBeTruthy();
    });
});
