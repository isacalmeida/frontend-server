import { ErroConhecidoDTO } from './erro-conhecido.dto';

describe('ErroConhecidoDTO', () => {
    it('should create an instance', () => {
        expect(new ErroConhecidoDTO()).toBeTruthy();
    });
});
