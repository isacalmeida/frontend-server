import { GeneroPessoaDTO } from './genero-pessoa.dto';

describe('GeneroPessoaDTO', () => {
    it('should create an instance', () => {
        expect(new GeneroPessoaDTO()).toBeTruthy();
    });
});
