import { StatusEventoDTO } from './status-evento.dto';

describe('StatusEventoDTO', () => {
    it('should create an instance', () => {
        expect(new StatusEventoDTO()).toBeTruthy();
    });
});
