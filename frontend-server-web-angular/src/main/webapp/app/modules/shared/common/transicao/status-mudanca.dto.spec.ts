import { StatusMudancaDTO } from './status-mudanca.dto';

describe('StatusMudancaDTO', () => {
    it('should create an instance', () => {
        expect(new StatusMudancaDTO()).toBeTruthy();
    });
});
