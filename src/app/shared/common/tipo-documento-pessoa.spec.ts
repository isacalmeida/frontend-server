import { TipoDocumentoPessoa } from './tipo-documento-pessoa';

describe('TipoDocumentoPessoa', () => {
  it('should create an instance', () => {
    expect(new TipoDocumentoPessoa()).toBeTruthy();
  });
});
